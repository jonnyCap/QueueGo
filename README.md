# QueueGo
QueueGo is a lightweight Pub/Sub message broker written in Go,. It communicates over a custom TCP protocol called Blink, designed for minimal overhead and real-time messaging. QueueGo supports both persistent and non-persistent queues, using BadgerDB for durable message storage, and includes support for JWT-based authentication.
